import Layout from 'components/layout';
import Table from 'components/table';

export default function Aire() {
    return (
        <Layout title={'Aire'}>
            <Table value={'air'} sign={'%'} />
        </Layout>
    );
}
